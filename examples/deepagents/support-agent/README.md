# DeepAgents Support Agent

Minimal runnable package for `DeepAgentsAdapter` smoke coverage.

- Entry point: `agent:build_agent`
- Runtime: deterministic `create_deep_agent(...)` graph
- Behavior: returns a stable response and streams real DeepAgents graph events
