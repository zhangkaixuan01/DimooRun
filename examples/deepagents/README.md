# DeepAgents Examples

这里放 DeepAgents Agent Package 示例。

示例必须覆盖：

- `create_deep_agent(config)` 入口
- invoke
- stream
- filesystem capability
- subagents capability
- AgentVersion manifest

当前目录对应 Phase 2：`DeepAgentsAdapter` 生产化后补充可运行示例。
