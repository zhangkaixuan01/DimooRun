# LangChain Agent Examples

这里放 LangChain Agent Package 示例。

示例必须覆盖：

- `create_agent(config)` 入口
- invoke
- stream
- tool call event mapping
- model usage / token usage
- AgentVersion manifest

当前目录对应 Phase 2：`LangChainAgentAdapter` 生产化后补充可运行示例。
