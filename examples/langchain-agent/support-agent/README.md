# LangChain Support Agent

Minimal runnable package for `LangChainAgentAdapter` smoke coverage.

- Entry point: `agent:build_agent`
- Runtime: deterministic `create_agent(...)` graph
- Behavior: returns a stable support response and supports `astream`
