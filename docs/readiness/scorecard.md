# Production Readiness Scorecard

This scorecard is the source of truth for production-grade claims. It tracks current evidence against `docs/plans/production-grade-gap-closure-2026-06-04.md` and must stay conservative: unverified work is `partial` or `missing`, even when code or routes exist.

Status values:

- `complete`: implemented and backed by direct current evidence.
- `partial`: meaningful progress exists, but the acceptance scope is not fully proven.
- `missing`: no durable implementation or proof exists yet.
- `blocked`: work cannot move without an external dependency or user decision.

## Milestone Status

| Milestone | Status | Evidence | Remaining gap |
|---|---|---|---|
| Milestone A: Internal Alpha | partial | User task model, experience acceptance, workflow matrix, frontend unit/e2e harness files, console aggregate API tests, readiness scorecard, product narrative docs, `.github/workflows/ci.yml`, and browser smoke report exist. Local `npm run test:e2e` passes with 34 Playwright tests using the ignored `.env.e2e.local` supplied Chrome path. | Successful live Compose smoke output, current hosted CI green run, screenshot evidence, default Playwright cache proof, and live-backend browser proof are not complete. |
| Milestone B: Production Beta | partial | Runtime, deployment, governance, package, worker, observability, Console foundations, production startup guards, and durable native idempotency exist with backend tests. | End-to-end runtime failure proof, browser workflow coverage, and high-risk action enforcement are incomplete across all core workflows. |
| Milestone C: External GA | missing | Some Docker, Helm, SDK, CLI, compatibility, identity, and admin surfaces exist. | Clean checkout Compose smoke, Kubernetes smoke, SDK contract proof, trust assets, release engineering, and GA workflow coverage are not complete. |
| Milestone D: Competitive Excellence | missing | Product optimization backlog identifies P0/P1/P2 differentiation items. | Guided activation, runtime workbench, action center, resource graph, evidence bundles, feedback capture, integration health center, and product telemetry are not implemented as complete workflows. |

## Early Phase Status

| Phase | Status | Evidence | Remaining gap |
|---|---|---|---|
| Phase -3: User Task And Experience Baseline | complete | `docs/product/console-user-task-model.md` and `docs/product/console-experience-acceptance.md` define roles, jobs, risks, pre-action context, recovery paths, and browser evidence requirements. | Keep the model synchronized as workflows graduate from generic CRUD to domain workflows. |
| Phase -2: Product Workflow Spec Reconciliation | partial | `docs/product/workflow-coverage-matrix.md`, `docs/product/function-coverage-review.md`, and `docs/product/optimization-backlog.md` classify workflows and optimization priorities. | This scorecard now exists, but it must be maintained as implementation and verification evidence changes. |
| Phase -1A: Frontend Test Harness Baseline | partial | `apps/console/playwright.config.ts`, `apps/console/vitest.config.ts`, unit tests, fixtures, and workflow e2e specs exist. Playwright config supports CI-managed Chromium and a local `DIMOORUN_PLAYWRIGHT_CHROME` fallback from `.env.e2e.local`. Local `npm run check:e2e-browser` and `npm run test:e2e` pass with 34 Playwright tests using the supplied Chrome executable. | Hosted CI/default Playwright cache proof, live-backend browser proof, and broader responsive/mobile evidence are still incomplete. |
| Phase -1B: Frontend State Architecture Baseline | partial | Shared API query and mutation helpers exist in `apps/console/src/api/query.ts` and `apps/console/src/api/mutations.ts` with unit tests. | Large pages still contain page-local workflow state and need broader adoption of stale-response guards, normalized error state, and reusable workflow state machines. |
| Phase -1C: Console Aggregate And Permission API Contract | partial | `/v1/console/*` aggregate endpoints and tests exist, including runtime overview and action summary. | Aggregates need broader workflow coverage for action center, impact preview, resource graph, identity, settings, gateway, quality, and operations. |
| Phase 1: Production Truth Baseline | partial | README states current maturity caveats and local verification baseline. `scripts/compose_smoke.py` verifies the Compose stack contract, `scripts/compose_runtime_smoke.py` can run the stack, and `.github/workflows/ci.yml` defines backend/docs/deployment-contract and Console jobs. `docs/readiness/compose-smoke-report.md` records the current local Docker daemon blocker. | Hosted CI green output, successful container Compose smoke output, Kubernetes smoke, and production safety checks are not fully proven from this scorecard. |
| Phase 2: Production Startup Guards | complete | `apps/server/dimoo_run/core/startup_checks.py` now rejects SQLite, in-memory runtime store, default object-store credentials, dev CORS origins, missing secret provider config, and dev API key mode when `DIMOORUN_RUNTIME_MODE=production`; `apps/server/dimoo_run/server.py` enforces the guard from `create_app`; `.env.example` documents the minimum secure production variables; and `docker-compose.yml` now pins the local stack to explicit `dev` runtime semantics instead of looking like a production-safe default. Local proof includes `uv run pytest -q tests/server/test_config.py tests/server/test_startup_checks.py tests/api/test_platform_settings_workflows.py tests/api/test_native_api.py` and targeted `ruff check` over the startup-guard files. | None at the phase boundary; later phases still need durable idempotency, end-to-end runtime failure proof, broader observability, and deployment smoke evidence. |
| Phase 3: Durable Idempotency And API Contracts | complete | `apps/server/dimoo_run/runtime/sqlalchemy_idempotency.py` now persists `idempotency_records` in SQLAlchemy mode; `apps/server/dimoo_run/api/native/runtime.py` defaults the SQLAlchemy runtime to the durable idempotency store; and `apps/server/dimoo_run/api/native/dependencies.py` now builds request-scoped SQLAlchemy runtimes without falling back to a process-local idempotency singleton. Local proof includes store-level replay/conflict persistence in `tests/runtime/test_sqlalchemy_idempotency.py`, route-level agent and deployment task idempotency coverage plus SQLAlchemy restart replay in `tests/api/test_native_api.py`, and durable runtime regression proof in `tests/production_foundation/test_native_api_durable_runtime.py`. | None at the phase boundary; later phases still need end-to-end worker failure proof, broader write-contract coverage outside native task creation, and hosted deployment/runtime evidence. |
| Phase 12A: Product Narrative Baseline | partial | `docs/README.md`, `docs/start/product-overview.md`, `docs/start/getting-started.md`, `docs/reference/concepts.md`, `docs/architecture/overview.md`, `docs/start/quickstart.md`, `docs/readiness/current-maturity.md`, `docs/readiness/screenshots.md`, `docs/architecture/adrs/0001-runtime-control-plane.md`, and docs quality tests exist. | README still needs a tighter first-screen product entry, screenshots are placeholders without generated image evidence, and the 15-minute happy path is not yet proven by Compose smoke output. |

## Product Workflow Phase Status

| Phase | Status | Evidence | Remaining gap |
|---|---|---|---|
| Phase 0A: Agent Package And Version Readiness Workflow | partial | Package validation API covers manifest/runtime/secret/capability validation, readiness token gate, and non-blocking dependency warnings. Package Registration page renders validation errors, missing secret refs, capabilities, warnings, token, and next action. Local mocked browser proof now covers package validation and readiness blockers. | Hosted CI/default Playwright cache proof, live backend browser proof, and package-to-deployment-to-run proof are not complete. |
| Phase 0B: Deployment Promotion And Rollback Workflow | partial | Promotion APIs cover preview, promote, rollback, required audit reasons, stale expected-version conflict, policy denial, rollback context, and active-run/queued-task impact warnings. Console workflow and local mocked e2e specs cover preview, promote, rollback, pause/resume, policy denial, and stale conflict. | Live runtime/worker proof for state changes, drain behavior, production rollback evidence, hosted CI/default Playwright cache proof, and live backend browser proof are not complete. |
| Phase 0C: Run Triage And Replay Comparison Workflow | partial | Replay comparison API covers source run, candidate version, replay config, output/error/event diff, provenance, dataset capture IDs, and candidate readiness blocking. Console triage/replay pages and local mocked e2e specs cover failed-run triage, comparison creation, dataset evidence save, and source immutability messaging. | Durable replay evidence bundles, audit-backed dataset capture, incident responder proof against a live runtime, hosted CI/default Playwright cache proof, and live backend browser proof are not complete. |
| Phase 0D: Policy And Human Approval Workbench | partial | Policy simulation covers draft/sample decision, matched resources, audit preview, and conflict warnings. Policy activation/rollback preserve versions, rollback target, actor/request audit context, and now reject missing audit reasons. Human-task decisions preserve requester, risk reason, diff, comment, and resume outcome. Local mocked browser proof covers policy simulation, activation, rollback, denial evidence, and approve/reject decisions. | Policy authoring remains shallow, stale approval guards and real runtime resume proof are incomplete, audit comparison is not complete, and hosted CI/default Playwright cache plus live backend browser proof are still pending. |
| Phase 0E: Gateway, Tool, And Secret Governance Workflow | partial | Model gateway test covers credential ref validation, safe health probe, budget preview, fallback preview, provider error normalization, and audit preview. Tool dry-run covers schema validation, risk classification, policy preview, approval requirement, usage-history link, and no-side-effect output. Secret validation/rotation return metadata without secret values, track last-used/access audit, and blocked invalid rotations no longer replace the current ref. Local mocked browser proof covers model gateway, tool gateway, and secret governance workflows. | Provider probes and secret backends are mocked/local, usage history is a link rather than durable data, policy binding to real runtime execution is not complete, and hosted CI/default Playwright cache plus live backend browser proof are still pending. |
| Phase 0F: Quality, Dataset, And Experiment Workflow | partial | Run-to-dataset capture covers redaction, provenance, scope, duplicate handling, and audit preview. Experiment execution covers evaluator config, candidate version, score distribution, and quality gate result. Quality gate preview blocks failed experiments and now blocks candidate/evidence mismatches before promotion. Local mocked browser proof covers dataset capture, experiment run, and promotion quality gate preview paths. | Evaluator execution is synthetic, durable quality evidence bundles are incomplete, deployment promotion is not yet hard-bound to quality gate results, and hosted CI/default Playwright cache plus live backend browser proof are still pending. |
| Phase 0G: Incident, Notification, Backup, And Restore Workflow | partial | Incident acknowledge/resolve APIs return timeline, linked evidence, delivery attempts, and audit context, and incident actions now block missing audit notes before creating timeline or delivery evidence. Incident acknowledge blocks malformed linked run/task/event evidence, including partially blank linked run or event evidence, before creating timeline or delivery evidence. Incident resolve also blocks missing resolution summaries, and incident actions block invalid notification-channel lists, including partially blank channel lists, before recording timeline or delivery evidence. Notification test-send records visible delivery-attempt evidence and blocks missing, malformed, or padded `target_ref`, blank messages, malformed or padded `channel_id`, or explicitly blank or padded channel names before creating a fake delivery attempt. Backup and restore dry-runs return operation-specific scope proof and now block project-scoped dry-runs that lack `X-Project-Id` and environment-scoped dry-runs that lack `X-Environment` with explicit scope error codes and `disabled_action_reason`. Backup dry-run blocks malformed, leading-zero string, signed-string, boolean, fractional, non-positive, or padded plan IDs, blank or unsupported scopes, missing targets, non-list target payloads, non-string target entries, padded targets, or partially blank targets, invalid non-object-storage refs, padded storage refs, bucket-only, query/fragment-marked, empty-segment, traversal-segment, or shallow object-storage refs without bucket/prefix identity, unsupported backup targets, and duplicate backup targets. Restore dry-run blocks invalid, padded, query/fragment-marked, trailing-slash, empty-segment, traversal-segment, or shallow non-identity `backup://` refs, backup refs without identity before the supported scope suffix, backup refs missing a supported scope suffix, blank or unsupported restore scopes, missing targets, non-list target payloads, non-string target entries, padded targets, or partially blank targets, unsupported restore targets, duplicate restore targets, clear backup-ref/restore-scope mismatches, malformed destructive flags, missing destructive confirmation, and now uses scope-specific destructive confirmation phrases for tenant restores. Local mocked browser proof covers incident acknowledge/resolve, notification probe, and backup/restore dry-run guardrails. | Provider-backed notification delivery is not proven, live backup/restore execution is not proven, backup manifest validation is still shallow beyond target/storage/ref format checks, runbooks are not embedded, durable incident/recovery evidence bundles are incomplete, and hosted CI/default Playwright cache plus live backend browser proof are still pending. |
| Phase 0H: Published Surface, Ingress, And Agent Gateway Workflow | partial | Publish validation, publish blocking, route testing, request-log drilldown, rollout controls, revoke confirmation, traffic split, shadow mode, rollback snapshot behavior, audit envelopes, permission summaries, impact previews, and Console workflow coverage exist. Backend API tests now include live HTTP ingress smoke through `/v1/ingress/{path}` after a governed publish, proving the published route binding can accept a real HTTP request, return trace/runtime-task evidence, generate a `req_live_*` request ID when clients omit `X-Request-Id`, emit `X-Request-Id` / `X-DimooRun-Trace-Id` correlation headers for accepted live ingress responses, emit `X-Request-Id` on blocked ingress responses, enforce published CORS allowed-origin policy with `Access-Control-Allow-Origin` / `Vary: Origin` for allowed origins and 403 evidence for disallowed origins, handle dynamic published-surface `OPTIONS` preflight with `Access-Control-Allow-Methods` / `Access-Control-Allow-Headers` without global Console CORS intercepting ingress, and persist redacted request-log read models that survive gateway workflow reset with matching evidence-bundle request IDs visible from Console detail and drilldown. Publish and live ingress invocation now evaluate active database-backed policies through the shared `PolicyEngine` path, block denied or approval-required publishes without creating rollout history, record policy-denied or approval-required ingress 403 request-log/evidence output, preserve `approval_required` decision evidence, record `allow_with_redaction` and `allow_with_limit` ingress policy effects in responses/request logs/evidence bundles, compose limit and redaction effects from multiple matching allow policies, enforce published-surface rate limits on the live HTTP ingress path with 429 `X-RateLimit-Limit`, `X-RateLimit-Remaining`, and `Retry-After` headers plus request-log/evidence-bundle rate-limit metadata, record live ingress traffic split / shadow-mode routing decisions in responses and request logs, attach rollback `live_gateway_verification` evidence proving the restored in-process route binding accepts the old route and rejects the replaced route, and expose an `exposure_health` read model from real `live_http` request logs plus revoked/disabled surface state. Request logs and rollout history now include evidence-bundle metadata with stable bundle IDs, resource references, policy/impact summaries, audit scope, traffic-control state, trace ID, and redaction policy; Console exposes an evidence-bundle catalog with export links and immutable `AuditLog` recorded status; Console exposes redacted JSON evidence-bundle export by bundle ID; evidence bundle creation is indexed in immutable `AuditLog` records; evidence-bundle export actions are written to immutable `AuditLog` records with redacted payload summaries; evidence-bundle archive actions persist retention policy, retained-until metadata, archive reason, and immutable `AuditLog` records; a dedicated `published_surface_bindings` store now persists published route binding state so live ingress and Console detail survive gateway workflow reset; a dedicated `published_surface_evidence_bundles` store now persists recorded/exported/archived lifecycle state; a dedicated `published_surface_request_logs` store now persists redacted request-log JSON with status, path, method, trace, request ID, and evidence-bundle metadata; and a dedicated `published_surface_rollouts` store now persists rollout history JSON with operation, request ID, and evidence-bundle metadata so Console history survives gateway workflow reset. Local `uv run pytest tests/api/test_published_surface_workflows.py -q` now passes all 63 backend 0H API tests in a bounded local run after dev/test schema initialization was moved from global auth reset into the published-surface gateway and policy endpoints that actually need it. Local mocked browser tests cover publish validation, blocked invalid route, publish, route test, exposure-health display, request-log drilldown, traffic split, revoke confirmation, and rollback using a locally supplied Chrome executable. CI now has an explicit `npm run test:e2e:0h` Published Surface browser workflow command after `npx playwright install --with-deps chromium`, writes isolated `test-results-0h` output, and uploads a dedicated `console-playwright-0h-report` artifact from `apps/console/playwright-report-0h`, so hosted runs can produce phase-specific default-cache browser evidence. A scripted live-backend proof path now exists as `npm run test:e2e:live`, with a real backend launcher, bounded `--check` hard timeout, taskkill timeout protection, bounded standalone cleanup command handling, static Console bundle serving, single-process `published-surface-live-smoke.mjs` browser workflow, and Windows cleanup guardrails covered by Console contract tests; that scripted workflow is now required to drive route test, request-log drilldown, traffic split, live ingress, revoke, and rollback assertions in one browser path. On 2026-06-09, `npm run cleanup:e2e:live` followed by `npm run test:e2e:live:local` completed successfully in a normal Windows terminal, and `npm run verify:e2e:live-report` accepted the resulting `%TEMP%\\dimoorun-console-live-e2e\\run-live-e2e.log`, proving the local real-browser live path can complete route test, request-log drilldown, traffic split, live ingress, revoke, rollback, and post-rollback ingress acceptance in one bounded run. Local `node scripts/start-live-backend.mjs --check` now proves the live backend launcher can start on `127.0.0.1:4180`, serve `/docs`, and exit cleanly without leaving the port bound; the launcher also fails fast if `4180` is already in use instead of treating a stale listener as success. | Browser proof still uses mocked API data and a locally supplied Chrome executable until a hosted CI artifact proves the default Playwright cache path; live ingress proof is still an in-process TestClient smoke rather than hosted gateway/runtime execution; hosted gateway/rate-limiter enforcement, hosted traffic split/shadow routing, hosted policy-composition enforcement proof, and hosted gateway rollback compatibility are incomplete. See `docs/readiness/phase-0h-evidence.md` for the exact remaining evidence checklist and operator commands. |
| Phase 0I: Compatibility Migration And Runtime Explorer Workflow | complete | Console compatibility explorer APIs now cover LangGraph assistant list/create/detail, thread create/detail, run create/detail, run join, cancel, stream status, and `Last-Event-ID` replay mapped onto native resources. Migration report output now explains unsupported capabilities, required DimooRun config, adapter contract version, checkpoint requirements, streaming support, and governance implications. The golden compatibility runner now records requested operation, structured expected third-party semantics, DimooRun response, native resources created, and divergence reason, and Console surfaces those records during migration and runtime exploration. Console compatibility explorer pages now provide a migration report panel, request builder, runtime result panel, native resource links, stream probe, replay input, refresh-backed reconnect state via query persistence, and unsupported capability explanations. Local backend proof includes `uv run pytest -q tests/compat/test_langgraph_compat_api.py tests/api/test_compatibility_console_workflows.py tests/compatibility/test_golden_runtime_alignment.py`. Local Console proof includes `npm run test`, `npm run test:unit`, `npm run build:e2e`, `npx playwright test tests/e2e/compatibility-explorer.spec.ts --project=chrome`, and the local CI-style wrapper `PLAYWRIGHT_HTML_REPORT=playwright-report-0i npm run test:e2e:0i`. Hosted CI published `console-playwright-0i-report` in successful runs `27225197478` (2026-06-09) and `27275225184` (2026-06-10), proving the dedicated workflow on the default Playwright-managed Chromium path. | Broader third-party migration proof against an externally hosted runtime belongs to later release hardening rather than this phase definition. |
| Phase 0J: Worker, Agent Instance, And Capacity Operations Workflow | complete | Worker and capacity APIs now expose heartbeat age, queues, drain status, version, capacity, active attempts, retry/dead-letter pressure, queue drilldown, worker actions, agent-instance detail, concurrency limit, runtime config hash, and recommended operator action. Worker snapshots are now persisted in the baseline schema as `worker_snapshots`, so worker heartbeat and drain state survive separate registry instances instead of depending on in-process memory only; the durable runtime `NativeTask` view now also carries `worker_id`, which keeps SQLAlchemy-backed worker assignments visible in worker/capacity read models. Capacity aggregation now scopes worker-attributed tasks to the selected environment instead of counting every task with a `worker_id`, worker control writes stay scoped to the requested tenant/project/environment even when `worker_id` values are reused, and worker detail no longer leaks active attempts from another environment that reused the same worker identifier. Local backend proof includes `uv run pytest -q tests/api/test_worker_capacity_console.py tests/api/test_console_aggregate_api.py` plus `uv run ruff check apps/server/dimoo_run/runtime/capacity.py apps/server/dimoo_run/api/console/workers.py apps/server/dimoo_run/domain/models.py apps/server/dimoo_run/api/native/runtime.py tests/api/test_worker_capacity_console.py migrations/versions/0001_baseline.py`. Local Console proof includes `npm run test`, `npm run test:unit`, `npm run build:e2e`, `npx playwright test tests/e2e/runtime-capacity.spec.ts --project=chrome --output test-results-0j`, and `npm run test:e2e:0j`. Hosted CI published `console-playwright-0j-report` in successful run `27275225184` (2026-06-10), proving the dedicated workflow on the default Playwright-managed Chromium path. | None at the phase boundary; later milestones still need broader runtime/operations proof. |
| Phase 0K: Identity, Role, Permission, Session, And Machine Identity Workflow | complete | Identity workflow APIs now cover role permission matrix preview/apply with change diff, affected operators, self-lockout warnings, and audit-required apply; operator access detail with roles, inherited permissions, active sessions, API keys created by the operator, recent audit actions, and disable impact; and service-account detail with API key scope diff, last-used metadata, dependent deployments, published surfaces, rotation, and forced expiry. Console now has a dedicated role matrix page, operator access detail page, and service-account detail page, while the existing operator and machine-identity list pages now link into those detail workflows. Local backend proof includes `uv run ruff check apps/server/dimoo_run/api/admin/identity_workflows.py apps/server/dimoo_run/api/console/identity.py apps/server/dimoo_run/identity/permission_matrix.py tests/api/test_identity_workflows.py tests/production_foundation/test_ci_workflow.py` and `uv run pytest -q tests/api/test_identity_workflows.py tests/production_foundation/test_ci_workflow.py`. Local Console proof includes `npm run test`, `npm run test:unit`, `npm run build:e2e`, and `npx playwright test tests/e2e/identity-governance.spec.ts --project=chrome --output test-results-0k-local`. Hosted CI published `console-playwright-0k-report` in successful run `27347574486` (2026-06-11), proving the dedicated clean-environment wrapper path on the default Playwright-managed Chromium cache. | None at the phase boundary; later milestones still need broader identity governance integration across the full product surface. |
| Phase 0L: Platform Settings, Providers, And Dangerous Configuration Workflow | complete | Console settings workflow APIs now cover platform settings snapshot, provider-status read models, scoped defaults, dangerous-action preflight, and dangerous-action apply with audit envelopes. The backend persists scoped configuration in `platform_control_settings`, computes production-safety warnings from real runtime settings, blocks organization/project writes when runtime mode is production, and exposes provider health for database, queue, object store, secret provider, model gateway, webhook transport, notification transport, and observability exporter. Console now has dedicated Platform Settings, Provider Status, and Danger Zone pages, while the original Settings page remains focused on personal preferences and links operators into the platform workflows. The Platform Settings page now renders organization/project/environment configuration boundaries directly, and the Danger Zone page now renders affected-resource preview plus apply result metadata instead of hiding those API details. Local backend proof includes `uv run pytest -q tests/api/test_platform_settings_workflows.py tests/production_foundation/test_ci_workflow.py` and `uv run mypy apps/server tests scripts`. Local Console proof includes `npm run test`, `npm run test:unit`, `npm run build:e2e`, the shared browser runner `npm run test:e2e:0j`, and the dedicated phase verifier `npm run test:e2e:0l`, where the verifier emits a phase-specific report from the shared runner proof marker. Hosted CI published `console-playwright-0l-report` in successful run `27347574486` (2026-06-11), proving the shared-runner-plus-verifier path on the default Playwright-managed Chromium cache. | None at the phase boundary; later milestones still need broader platform/runtime production hardening beyond this workflow. |

## Milestone A Exit Criteria

| Exit criterion | Status | Evidence | Remaining gap |
|---|---|---|---|
| Current readiness scorecard exists and does not claim missing work as complete. | complete | This document records explicit `partial` and `missing` statuses. | Maintain in the same change when workflow verdicts move. |
| CI has a green backend/frontend baseline. | partial | `.github/workflows/ci.yml` defines backend/docs/deployment-contract checks and Console contract/unit/build/e2e checks, and `tests/production_foundation/test_ci_workflow.py` verifies required commands and Playwright browser/config consistency. | A hosted CI run has not yet proven the current worktree green. |
| Compose smoke is specified, even if it initially documents failures. | partial | `uv run python scripts\compose_smoke.py` passes as a static Compose contract check. `uv run python scripts\compose_runtime_smoke.py` is documented for live container smoke. `docs/readiness/compose-smoke-report.md` records the current `blocked-by-local-docker-daemon` result. | Run and record successful live Docker Compose smoke output from a Docker-enabled clean checkout. |
| README and quickstart describe the real maturity status. | partial | README includes maturity caveats, and `docs/start/quickstart.md` plus `docs/readiness/current-maturity.md` describe current limits. | The quickstart path still needs clean-machine execution evidence and generated screenshots before this exit criterion is complete. |
| Browser smoke and critical accessibility baseline pass. | partial | Playwright and axe specs exist under `apps/console/tests/e2e`. Local `npm run check:e2e-browser` reads the supplied Chrome path from `.env.e2e.local`, and local `npm run test:e2e` passes with 34 Playwright tests including critical axe checks for dashboard, login, dense table, drawer flow, and high-risk confirmation. `docs/readiness/browser-smoke-report.md` records the current local pass evidence. | Hosted CI/default Playwright cache proof, live-backend browser proof, mobile viewport coverage, and generated screenshot evidence are still incomplete. |

## Claim Guardrails

DimooRun must not be described as production-ready, externally GA-ready, or a perfect production-grade platform until every Definition of Done item in the gap closure plan is directly proven. Product documents should use the current wording:

```text
Production-shaped foundation: yes.
External production-grade platform: not yet.
```

When evidence improves, update the relevant row with the exact command, artifact, test file, browser evidence, or runtime proof that supports the change.
