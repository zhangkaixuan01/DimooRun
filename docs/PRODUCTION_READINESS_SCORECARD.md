# Production Readiness Scorecard

This scorecard is the source of truth for production-grade claims. It tracks current evidence against `docs/PRODUCTION_GRADE_GAP_CLOSURE_PLAN_2026-06-04.md` and must stay conservative: unverified work is `partial` or `missing`, even when code or routes exist.

Status values:

- `complete`: implemented and backed by direct current evidence.
- `partial`: meaningful progress exists, but the acceptance scope is not fully proven.
- `missing`: no durable implementation or proof exists yet.
- `blocked`: work cannot move without an external dependency or user decision.

## Milestone Status

| Milestone | Status | Evidence | Remaining gap |
|---|---|---|---|
| Milestone A: Internal Alpha | partial | User task model, experience acceptance, workflow matrix, frontend unit/e2e harness files, console aggregate API tests, readiness scorecard, product narrative docs, `.github/workflows/ci.yml`, and browser smoke report exist. | Successful live Compose smoke output, current hosted CI green run, screenshot evidence, and passing browser smoke/accessibility output are not complete. |
| Milestone B: Production Beta | partial | Runtime, deployment, governance, package, worker, observability, and Console foundations exist with backend tests. | Production startup guards, end-to-end runtime failure proof, browser workflow coverage, and high-risk action enforcement are incomplete across all core workflows. |
| Milestone C: External GA | missing | Some Docker, Helm, SDK, CLI, compatibility, identity, and admin surfaces exist. | Clean checkout Compose smoke, Kubernetes smoke, SDK contract proof, trust assets, release engineering, and GA workflow coverage are not complete. |
| Milestone D: Competitive Excellence | missing | Product optimization backlog identifies P0/P1/P2 differentiation items. | Guided activation, runtime workbench, action center, resource graph, evidence bundles, feedback capture, integration health center, and product telemetry are not implemented as complete workflows. |

## Early Phase Status

| Phase | Status | Evidence | Remaining gap |
|---|---|---|---|
| Phase -3: User Task And Experience Baseline | complete | `docs/CONSOLE_USER_TASK_MODEL.md` and `docs/CONSOLE_EXPERIENCE_ACCEPTANCE.md` define roles, jobs, risks, pre-action context, recovery paths, and browser evidence requirements. | Keep the model synchronized as workflows graduate from generic CRUD to domain workflows. |
| Phase -2: Product Workflow Spec Reconciliation | partial | `docs/PRODUCT_WORKFLOW_COVERAGE_MATRIX.md`, `docs/PRODUCT_FUNCTION_COVERAGE_REVIEW.md`, and `docs/PRODUCT_OPTIMIZATION_BACKLOG.md` classify workflows and optimization priorities. | This scorecard now exists, but it must be maintained as implementation and verification evidence changes. |
| Phase -1A: Frontend Test Harness Baseline | partial | `apps/console/playwright.config.ts`, `apps/console/vitest.config.ts`, unit tests, fixtures, and workflow e2e specs exist. Playwright config now uses the Chromium browser installed by CI instead of forcing the branded Chrome channel. | Local browser execution is blocked by Playwright Chromium install timeout; browser evidence is still uneven across workflow success, loading, empty, API error, permission denial, destructive confirmation, accessibility, and responsive paths. |
| Phase -1B: Frontend State Architecture Baseline | partial | Shared API query and mutation helpers exist in `apps/console/src/api/query.ts` and `apps/console/src/api/mutations.ts` with unit tests. | Large pages still contain page-local workflow state and need broader adoption of stale-response guards, normalized error state, and reusable workflow state machines. |
| Phase -1C: Console Aggregate And Permission API Contract | partial | `/v1/console/*` aggregate endpoints and tests exist, including runtime overview and action summary. | Aggregates need broader workflow coverage for action center, impact preview, resource graph, identity, settings, gateway, quality, and operations. |
| Phase 1: Production Truth Baseline | partial | README states current maturity caveats and local verification baseline. `scripts/compose_smoke.py` verifies the Compose stack contract, `scripts/compose_runtime_smoke.py` can run the stack, and `.github/workflows/ci.yml` defines backend/docs/deployment-contract and Console jobs. `docs/COMPOSE_SMOKE_REPORT.md` records the current local Docker daemon blocker. | Hosted CI green output, successful container Compose smoke output, Kubernetes smoke, and production safety checks are not fully proven from this scorecard. |
| Phase 12A: Product Narrative Baseline | partial | `docs/README.md`, `docs/PRODUCT_OVERVIEW.md`, `docs/GETTING_STARTED.md`, `docs/CONCEPTS.md`, `docs/ARCHITECTURE.md`, `docs/QUICKSTART.md`, `docs/CURRENT_MATURITY.md`, `docs/SCREENSHOTS.md`, `docs/ADRS/0001-runtime-control-plane.md`, and docs quality tests exist. | README still needs a tighter first-screen product entry, screenshots are placeholders without generated image evidence, and the 15-minute happy path is not yet proven by Compose smoke output. |

## Product Workflow Phase Status

| Phase | Status | Evidence | Remaining gap |
|---|---|---|---|
| Phase 0A: Agent Package And Version Readiness Workflow | partial | Package validation API covers manifest/runtime/secret/capability validation, readiness token gate, and non-blocking dependency warnings. Package Registration page renders validation errors, missing secret refs, capabilities, warnings, token, and next action. | Local browser execution is blocked by Playwright Chromium install, and package-to-deployment-to-run proof is not complete. |
| Phase 0B: Deployment Promotion And Rollback Workflow | partial | Promotion APIs cover preview, promote, rollback, required audit reasons, stale expected-version conflict, policy denial, rollback context, and active-run/queued-task impact warnings. Console workflow and e2e specs cover preview, promote, rollback, pause/resume, policy denial, and stale conflict with mocked API data. | Local browser execution is blocked by Playwright Chromium install, and live runtime/worker proof for state changes, drain behavior, and production rollback evidence is not complete. |
| Phase 0C: Run Triage And Replay Comparison Workflow | partial | Replay comparison API covers source run, candidate version, replay config, output/error/event diff, provenance, dataset capture IDs, and candidate readiness blocking. Console triage/replay pages and mocked e2e specs cover failed-run triage, comparison creation, dataset evidence save, and source immutability messaging. | Local browser execution is blocked by Playwright Chromium install, durable replay evidence bundles and audit-backed dataset capture are incomplete, and incident responder proof against a live runtime is not complete. |
| Phase 0D: Policy And Human Approval Workbench | partial | Policy simulation covers draft/sample decision, matched resources, audit preview, and conflict warnings. Policy activation/rollback preserve versions, rollback target, actor/request audit context, and now reject missing audit reasons. Human-task decisions preserve requester, risk reason, diff, comment, and resume outcome. | Local browser execution is blocked by Playwright Chromium install, policy authoring remains shallow, stale approval guards and real runtime resume proof are incomplete, and audit comparison is not complete. |
| Phase 0E: Gateway, Tool, And Secret Governance Workflow | partial | Model gateway test covers credential ref validation, safe health probe, budget preview, fallback preview, provider error normalization, and audit preview. Tool dry-run covers schema validation, risk classification, policy preview, approval requirement, usage-history link, and no-side-effect output. Secret validation/rotation return metadata without secret values, track last-used/access audit, and blocked invalid rotations no longer replace the current ref. | Local browser execution is blocked by Playwright Chromium install, provider probes and secret backends are mocked/local, usage history is a link rather than durable data, and policy binding to real runtime execution is not complete. |
| Phase 0F: Quality, Dataset, And Experiment Workflow | partial | Run-to-dataset capture covers redaction, provenance, scope, duplicate handling, and audit preview. Experiment execution covers evaluator config, candidate version, score distribution, and quality gate result. Quality gate preview blocks failed experiments and now blocks candidate/evidence mismatches before promotion. | Local browser execution is blocked by Playwright Chromium install, evaluator execution is synthetic, durable quality evidence bundles are incomplete, and deployment promotion is not yet hard-bound to quality gate results. |
| Phase 0G: Incident, Notification, Backup, And Restore Workflow | partial | Incident acknowledge/resolve APIs return timeline, linked evidence, delivery attempts, and audit context, and incident actions now block missing audit notes before creating timeline or delivery evidence. Incident acknowledge blocks malformed linked run/task/event evidence, including partially blank linked run or event evidence, before creating timeline or delivery evidence. Incident resolve also blocks missing resolution summaries, and incident actions block invalid notification-channel lists, including partially blank channel lists, before recording timeline or delivery evidence. Notification test-send records visible delivery-attempt evidence and blocks missing, malformed, or padded `target_ref`, blank messages, malformed or padded `channel_id`, or explicitly blank or padded channel names before creating a fake delivery attempt. Backup and restore dry-runs return operation-specific scope proof and now block project-scoped dry-runs that lack `X-Project-Id` and environment-scoped dry-runs that lack `X-Environment` with explicit scope error codes and `disabled_action_reason`. Backup dry-run blocks malformed, leading-zero string, signed-string, boolean, fractional, non-positive, or padded plan IDs, blank or unsupported scopes, missing targets, non-list target payloads, non-string target entries, padded targets, or partially blank targets, invalid non-object-storage refs, padded storage refs, bucket-only, query/fragment-marked, empty-segment, traversal-segment, or shallow object-storage refs without bucket/prefix identity, unsupported backup targets, and duplicate backup targets. Restore dry-run blocks invalid, padded, query/fragment-marked, trailing-slash, empty-segment, traversal-segment, or shallow non-identity `backup://` refs, backup refs without identity before the supported scope suffix, backup refs missing a supported scope suffix, blank or unsupported restore scopes, missing targets, non-list target payloads, non-string target entries, padded targets, or partially blank targets, unsupported restore targets, duplicate restore targets, clear backup-ref/restore-scope mismatches, malformed destructive flags, missing destructive confirmation, and now uses scope-specific destructive confirmation phrases for tenant restores. | Local browser execution is blocked by Playwright Chromium install, provider-backed notification delivery is not proven, live backup/restore execution is not proven, backup manifest validation is still shallow beyond target/storage/ref format checks, runbooks are not embedded, and durable incident/recovery evidence bundles are incomplete. |
| Phase 0H: Published Surface, Ingress, And Agent Gateway Workflow | partial | Publish validation API covers route path, including traversal/query/fragment route path blocking, auth mode, deployment binding, environment scope, CORS wildcard and malformed `allowed_origins` shape blocking, rate limit policy, policy enforcement, audit context, malformed, fractional, or leading-zero numeric deployment/rate-limit fields without server errors, invalid environment scope blocking, publish-time malformed surface ID blocking without creating a surface or rollout, publish-blocked responses with deny decision context and blocked audit envelopes, and validation-failed publish requests no longer append rollout history. Route test API returns matched deployment, auth decision, policy decision, expected runtime task shape, request log, trace ID, traffic-control state, and redacted request metadata, uses the published surface deployment/environment/auth-mode binding in route-test output, and blocks malformed route identifiers, unmatched paths, unsupported methods, disabled or revoked surfaces, and synthetic rate-limit exceedance without server errors. Request logs include deployment/environment/auth-mode binding, run/task linkage, trace ID, auth/policy results, latency, status, redacted request metadata, traffic split and shadow-mode evidence, rate-limit 429 denial evidence, and inactive-surface 403 denial evidence without runtime task linkage. Console aggregate returns deployment binding health, request logs, single request-log drilldown with scoped 404 behavior, rollout history, dangerous revoke confirmation metadata, action audit-required flags, permission summaries, impact previews with affected resources and last known health, synthetic action policy decisions, audit previews, and rollback recovery-path metadata, and the Console route contract now opens `/published-surfaces/:surfaceId` directly against the matching surface detail workflow. Console workflow and mocked browser tests now cover publish validation, blocked invalid route, publish, route test, redacted request-log drilldown, traffic split, revoke confirmation, and rollback using a locally supplied Chrome executable. Rollout controls cover traffic split, route shadow mode with route-level audit evidence, publish and rollout-history action snapshots with retained API scope and action decision context, successful and blocked rollout responses with scoped audit envelopes, blocked inactive traffic-control responses with deny decision context, missing audit-reason responses with deny decision context for rollout controls including revoke, revoke confirmation-required responses with deny decision context, repeated revoke/disable/enable and revoked-surface enable blockers with deny decision context, unsupported operation blockers with deny decision context and `unsupported_operation_blocked` runtime effect, invalid traffic-split, route shadow-mode, and rollback-target responses with deny decision context for malformed and unknown targets, invalid route shadow-mode blocking without history entries, invalid traffic-split blocking without history entries, fractional or leading-zero traffic-split percentage blocking without history entries, required audit reasons for rollout control actions including revoke, unsupported operation blocking without history entries, revoke confirmation, already-revoked disabled-action reason with duplicate revoke blocking, revoked-surface disabled-action reasons for enable/disable/traffic-split/shadow-mode controls, disabled-surface disabled-action reasons for repeated disable and inactive traffic-split/shadow-mode controls, backend blocking of inactive traffic-split/shadow-mode attempts without rollout history entries, backend blocking of repeated enable/disable and revoked-surface enable without duplicate rollout history, snapshot-backed rollback to a previous published route/deployment/environment/auth binding, invalid rollback target blocking without history entries, and unknown rollback target blocking without history entries. | Browser proof currently uses mocked API data and a locally supplied Chrome executable rather than CI/default Playwright cache proof; request logs, blocked-action decision context, rollout-history action decision snapshots, action policy decisions, permission summaries, impact previews, and gateway decisions are synthetic/in-memory, real ingress traffic and policy engine integration are not proven, rollout traffic splitting and shadow routing remain synthetic route-test evidence rather than live gateway routing, rate limiting is in-memory synthetic evidence rather than a gateway enforcement layer, rollback compatibility is proven only against the synthetic surface snapshot rather than a live gateway deployment, and durable audit/evidence bundles are incomplete. |

## Milestone A Exit Criteria

| Exit criterion | Status | Evidence | Remaining gap |
|---|---|---|---|
| Current readiness scorecard exists and does not claim missing work as complete. | complete | This document records explicit `partial` and `missing` statuses. | Maintain in the same change when workflow verdicts move. |
| CI has a green backend/frontend baseline. | partial | `.github/workflows/ci.yml` defines backend/docs/deployment-contract checks and Console contract/unit/build/e2e checks, and `tests/production_foundation/test_ci_workflow.py` verifies required commands and Playwright browser/config consistency. | A hosted CI run has not yet proven the current worktree green. |
| Compose smoke is specified, even if it initially documents failures. | partial | `uv run python scripts\compose_smoke.py` passes as a static Compose contract check. `uv run python scripts\compose_runtime_smoke.py` is documented for live container smoke. `docs/COMPOSE_SMOKE_REPORT.md` records the current `blocked-by-local-docker-daemon` result. | Run and record successful live Docker Compose smoke output from a Docker-enabled clean checkout. |
| README and quickstart describe the real maturity status. | partial | README includes maturity caveats, and `docs/QUICKSTART.md` plus `docs/CURRENT_MATURITY.md` describe current limits. | The quickstart path still needs clean-machine execution evidence and generated screenshots before this exit criterion is complete. |
| Browser smoke and critical accessibility baseline pass. | partial | Playwright and axe specs exist under `apps/console/tests/e2e`. `npm run build:e2e` passes locally. `docs/BROWSER_SMOKE_REPORT.md` records the current Playwright Chromium install blocker. | `npm run test:e2e` has not run to completion locally or in hosted CI, so browser smoke and critical accessibility cannot be marked complete. |

## Claim Guardrails

DimooRun must not be described as production-ready, externally GA-ready, or a perfect production-grade platform until every Definition of Done item in the gap closure plan is directly proven. Product documents should use the current wording:

```text
Production-shaped foundation: yes.
External production-grade platform: not yet.
```

When evidence improves, update the relevant row with the exact command, artifact, test file, browser evidence, or runtime proof that supports the change.
